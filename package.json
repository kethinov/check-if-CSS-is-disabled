{
  "name": "check-if-css-is-disabled",
  "description": "Frontend JavaScript module that can determine if CSS is disabled in the browser.",
  "author": "Eric Newport <kethinov@gmail.com>",
  "version": "1.0.0",
  "homepage": "https://github.com/kethinov/check-if-CSS-is-disabled",
  "license": "CC-BY-4.0",
  "main": "cssIsDisabled.js",
  "readmeFilename": "README.md",
  "dependencies": {},
  "devDependencies": {
    "browserify": "~16.2.3",
    "husky": "~2.4.0",
    "lint-staged": "~8.2.0",
    "standard": "~12.0.1"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/kethinov/check-if-CSS-is-disabled.git"
  },
  "keywords": [
    "CSS",
    "disabled",
    "progressive enhancement",
    "PE"
  ],
  "scripts": {
    "bundle": "browserify -r ./cssIsDisabled.js -o dist/cssIsDisabled.js",
    "lint": "standard"
  },
  "lint-staged": {
    "*.js": "standard"
  },
  "standard": {
    "ignore": [
      "dist/"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
